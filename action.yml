name: 'buf-setup'
description: >
  Install buf for use in other jobs.
branding:
  icon: play
  color: blue
inputs:
  version:
    description: 'The version of buf to setup.'
    default: '1.0.0-rc5'
runs:
  using: 'node12'
  main: './dist/main.js'
